< js process >

1. 변수
2. 자료형(태/식)
3. 자료형 변환 = 형변환

//자료형
> 변수에 담겨질 수 있는 값의 형태를 의미
> 숫자형 / 문자열 / 논리형 / 객체 / 배열 / 심벌 / 함수
> 객체 => { key:value } => property (*프로퍼티 = 속성)

// 객체 > 배열

// index & length
// 사람  => 값을 입력 => 0, 1
// 비트 => 0, 1
// 바이트 

// js = "객체 지향" 프로그래밍 언어
> "절차 지향" => 고차함수

*심벌 > 유일무이한 값을 저장하고 관리하고 싶은 경우 사용

// 비교연산자 => 서로 다른 피연산자를 비교할 때 사용하는 연산자

*함수
익명함수 - 함수명을 변수로 대체하는 것 
// 함수는 이렇게 작성한느 것이 기본문법
> function fnc01() {}

*prompt( ) : 사용자에게 어떤 값을 받아올 수 있도록 해주는 함수
> 사용자가 아무리 숫자를 입력 => 문자열 형변환

// 문자형 + 숫자형 => 문자열
// - / * =>

//Numver( ) : 매개변수로 들어오는 값을 무조건 숫자로 형변환
** true = 1 / false = 0

//Number() vs parseInt() vs parseFloat()

// 소괄호 안에 들어온 값을 무조건 다 숫자로 바꿀 수 없음! 

*parseInt
=>숫자를 정수(*소수점이 없는 값)로 바꿔주는 역할

*parseFloat
=> 소괄호 안에 들어온 값을 실수의 형태로 바꿔주는 역할

// 쇼핑몰 10만개 의류 => 바코드 (*문자)

// null & undefined

** 문자 ==> 숫자 Number( )
** 숫자 ==> 문자 String( )
** 문자 & 숫자 => 논리 Boolean( )


*html&script 연결하는 방식
-인라인 스크립트
-내부 스크립트
-외부 스크립트
>웹 브라우저가 개발자가 작성한 html 문서를 읽는다

4. 연산자
- 산술연산자 :
- 연결연산자
- 할당연산자
- 비교연산자
- 논리연산자
(*삼항 조건연산자)

query : 질의하다. 질문하다












