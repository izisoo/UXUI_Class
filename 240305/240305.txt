*filter 함수 : 특정 카테고리 해당되는 상품만 출력
*reduce함수 : 체크박스를 클릭
> 상품의 전체 총 금액
> 배열!!! / 배열값 추가 => push / unshift

15.복습 & ES6 문법
- 객체 & 배열 복습
- ES6 업그레이드된 문법

1) 함수
> 매개변수 기본값 설정
> 나머지 매개변수 사용하는 방법

2) 배열
> 서로다른 배열을 하나로 합치는 방법 : 전개연산자[...arr, ...arr]
> 배열을 복제하는 방법 : [...arr] => 원.사본 배열의 연관관계x

3)객체
> value를 찾아오는 방법 2 : 온점표기법/대괄호표기법(*문자열)
> 객체 key 생성 시, 외부 함수를 활용

4) 심볼
> 심볼 값을 찾아올 때에는 반드시 [](*대괄화) 표시로 찾아와야 한다
> 심볼 전역요소로 생성 / 찾아올 수 있음

5) 구조분해할당
> 배열 및 객체 가능
> 구조분해할당 하고자하는 자료형의 타입을 값을 받는 요소에서도 동일하게 사용
> 객체의 경우, 반드시 변수명 === 객체의 key 이름과 동일
> 배열의 경우, 전개연산자를 활용해서 구조분해할당 가능
> 중첩객체에서 구조분해할당 하는 방법 => 가능




















